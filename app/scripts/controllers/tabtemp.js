'use strict';

/**
 * @ngdoc function
 * @name griddirApp.controller:TabtempCtrl
 * @description
 * # TabtempCtrl
 * Controller of the griddirApp
 */
angular.module('griddirApp')
  .controller('TabtempCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
